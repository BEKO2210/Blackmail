/**
 * AEGIS hosted service configuration.
 *
 * After you deploy the Cloudflare Worker (see service/README.md), paste its
 * public URL here — e.g. "https://aegis-switch.YOURNAME.workers.dev".
 * Everyone who visits this site will then use that one shared backend, so
 * users need ZERO technical setup of their own.
 *
 * Leave empty to show a "service not configured yet" notice.
 */
window.AEGIS_SERVICE_API = "";
